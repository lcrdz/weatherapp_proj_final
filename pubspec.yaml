name: weather_app_v2_proj
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  collection: ^1.17.1
  flutter:
    sdk: flutter
  geolocator: ^9.0.2
  http: ^1.1.0
  intl: ^0.18.1
  permission_handler: ^10.4.3
  syncfusion_flutter_charts: ^22.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/clouds.jpg
  uses-material-design: true